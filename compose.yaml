services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: masstamilan-express-api
    ports:
      - "3000:3000"
    restart: unless-stopped
    env_file:
      - .env.local
